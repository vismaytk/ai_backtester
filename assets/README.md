# Screenshots Directory

Place your app screenshots here for the README:
- `home.png` — Home screen screenshot
- `results.png` — Backtest results screenshot

To capture screenshots:
1. Run the app: `python -m streamlit run app.py`
2. Take screenshots of the home page and a backtest result
3. Save them here as `home.png` and `results.png`
